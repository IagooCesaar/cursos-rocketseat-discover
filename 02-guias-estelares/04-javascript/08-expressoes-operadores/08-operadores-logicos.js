// Operadores lógicos (logical operators)

let pao = true;
let queijo = true;

let cafe = true;
let leite = false;

// Operador AND &&
console.log(pao && queijo);
console.log(cafe && leite);

// Operador OR ||
console.log(pao || queijo);
console.log(cafe || leite);

// Operador NOT !
console.log(!pao);
console.log(!leite);
